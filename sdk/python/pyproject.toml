[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vipercapture"
version = "0.1.0"
description = "Dependency-free Python client for ViperCapture"
requires-python = ">=3.11"
license = "MIT"
